[project]
name = "incus-windows"
version = "0.1.0"
description = "Ansible-based Incus Windows image builder"
requires-python = ">=3.11"
dependencies = [
  "ansible-core>=2.19,<2.20",
]

[tool.uv]
package = false
